import type { NextPage } from 'next';

const Home: NextPage = () => (
  <div className="font-bold text-9xl flex justify-center items-center w-screen h-screen bg-slate-900 text-slate-50">
    Ax Studios
  </div>
);

export default Home;
