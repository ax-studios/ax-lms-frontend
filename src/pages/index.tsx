import type { NextPage } from 'next';

const Home: NextPage = () => (
  <div className="font-bold text-9xl flex justify-center items-center w-screen h-screen">
    Ax Studios
  </div>
);

export default Home;
